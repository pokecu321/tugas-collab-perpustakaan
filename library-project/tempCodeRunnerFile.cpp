ifstream bacafile("buku.txt"); 
            // string baris;
            // string judul;
            // cout << endl<<"cari!"<<endl
            //     << "masukkan judul buku : ";
            // getline(cin,judul);

            // while (getline(bacafile,baris))
            // {
            //     size_t posisi = baris.find(judul);
            // }